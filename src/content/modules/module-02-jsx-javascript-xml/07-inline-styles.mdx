---
id: "m02-07"
number: "07"
title: "Inline Styles"
moduleId: "module-02"
moduleNumber: "02"
category: "Fundamentals"
summary: "Passing style objects with camelCase keys to the style attribute."
codeTitle: "Inline Style Object in JSX"
---

# Inline Styles

Inline styles in JSX require a JavaScript object where CSS properties are camelCased (e.g., `backgroundColor` instead of `background-color`) and numeric units default to pixels (`px`).

### Key Concepts & Rules:

- Inline styles use double curly braces: `style={{ margin: 16, backgroundColor: '#000' }}`.
- Inline styles do not support media queries or pseudo-classes (:hover, :focus); use Tailwind CSS or CSS modules instead.

### Code Example: Inline Style Object in JSX

```tsx
export function CustomProgressBar({ progress }: { progress: number }) {
  return (
    <div className="w-full bg-slate-800 h-3 rounded-full overflow-hidden">
      <div 
        className="h-full bg-blue-500 transition-all duration-300"
        style={{ width: `${Math.min(100, Math.max(0, progress))}%` }}
      />
    </div>
  );
}
```
