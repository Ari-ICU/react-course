---
id: "m04-10"
number: "10"
title: "Default Props"
moduleId: "module-04"
moduleNumber: "04"
category: "Fundamentals"
summary: "ការកំណត់ fallback values សម្រាប់ optional props ដោយប្រើ ES6 default parameters។"
codeTitle: "ES6 Default Props Pattern"
---

# Default Props

ក្នុង modern React សូមប្រើប្រាស់ JavaScript ES6 default parameter values ដោយផ្ទាល់នៅត្រង់ destructured props signature ជំនួសឱ្យការប្រើ static property `defaultProps` ដែលហួសសម័យ (deprecated/legacy)។

### Key Concepts & Rules:

- ES6 defaults ផ្តល់នូវ syntax ខ្លី និងងាយស្រួលយល់សម្រាប់ props ដែលអាចមាន ឬគ្មាន (Optional)។
- Fallback value នឹងត្រូវយកមកប្រើ លុះត្រាតែ prop នោះមានតម្លៃ `undefined`។
- កាត់បន្ថយ runtime bugs នៅពេលដែល parent មិនបានបញ្ជូន prop ណាមួយមក។

### Code Example: ES6 Default Props Pattern

```jsx
// Fallbacks assigned directly in destructuring:
export function Button({ 
  label, 
  variant = "primary", 
  size = "md" 
}) {
  return <button className={`btn-${variant} btn-${size}`}>{label}</button>;
}
```
