---
id: "m07-08"
number: "08"
title: "Multiple State Variables"
moduleId: "module-07"
moduleNumber: "07"
category: "Core Concepts"
summary: "ពេលណាដែលត្រូវបំបែក state ជាច្រើន និងពេលណាដែលគួរប្រមូលផ្តុំជា object តែមួយ។"
codeTitle: "Multiple State Slices"
---

# Multiple State Variables

អ្នកអាចហៅ `useState` ច្រើនដងនៅក្នុង component តែមួយបាន។ គោលការណ៍ល្អគឺត្រូវបំបែក state ណាដែលមិនទាក់ទងគ្នាឱ្យនៅជាអថេរឯករាជ្យដាច់ដោយឡែកពីគ្នា។ វិធីនេះជួយឱ្យកូដងាយស្រួលអាន ងាយស្រួលថែទាំ និងងាយស្រួល refactor ទៅថ្ងៃមុខ។

### Key Concepts & Rules:

- អថេរ State ដាច់ដោយឡែកពីគ្នាធ្វើឱ្យ logic មានភាពសាមញ្ញ និងកាត់បន្ថយភាពស្មុគស្មាញនៃការ update។
- ដាក់បញ្ចូលគ្នាជា Object លុះត្រាតែវាជា fields នៃ Form តែមួយ ឬតម្លៃដែលផ្លាស់ប្តូរទន្ទឹមគ្នាជានិច្ច។
- ជួយជៀសវាងការ spread properties ច្រើនជាន់ដោយមិនចាំបាច់។

### Code Example: Multiple State Slices

```jsx
import React, { useState } from 'react';

export function UserSearchFilter() {
  // Independent concerns separated into distinct state slices:
  const [query, setQuery] = useState('');
  const [roleFilter, setRoleFilter] = useState('all');
  const [isSearching, setIsSearching] = useState(false);

  const handleReset = () => {
    setQuery('');
    setRoleFilter('all');
    setIsSearching(false);
  };

  return (
    <div className="p-5 bg-slate-900 border border-slate-800 rounded-xl space-y-4">
      <div className="grid grid-cols-1 sm:grid-cols-2 gap-3">
        <input
          type="text"
          value={query}
          onChange={(e) => setQuery(e.target.value)}
          placeholder="Search by name..."
          className="px-3 py-2 bg-slate-800 border border-slate-700 rounded-lg text-white text-sm"
        />

        <select
          value={roleFilter}
          onChange={(e) => setRoleFilter(e.target.value)}
          className="px-3 py-2 bg-slate-800 border border-slate-700 rounded-lg text-white text-sm"
        >
          <option value="all">All Roles</option>
          <option value="admin">Admins</option>
          <option value="member">Members</option>
        </select>
      </div>

      <div className="flex items-center justify-between text-xs text-slate-400">
        <span>Active Filter: {roleFilter} | Query: "{query}"</span>
        <button onClick={handleReset} className="text-blue-400 hover:underline">
          Reset All
        </button>
      </div>
    </div>
  );
}
```
