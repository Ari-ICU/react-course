---
id: "m13-02"
number: "02"
title: "React Router"
moduleId: "module-13"
moduleNumber: "13"
category: "Routing & Network"
summary: "បណ្ណាល័យស្តង់ដារសម្រាប់គ្រប់គ្រង declarative client-side routing ក្នុង React SPAs។"
codeTitle: "React Router Setup with RouterProvider"
---

# React Router

**React Router** គឺជាបណ្ណាល័យស្តង់ដារដ៏ពេញនិយមបំផុតសម្រាប់គ្រប់គ្រង Routing នៅក្នុងកម្មវិធី React SPA។ វាធ្វើការផ្គូផ្គង UI Components ទៅនឹង URL ជាក់លាក់នៅលើ Browser Address Bar និងផ្តល់នូវ Components និង Hooks ជាច្រើនដើម្បីរៀបចំរចនាសម្ព័ន្ធទំព័រ។

### Key Concepts & Rules:

- ធ្វើសមកាលកម្ម UI ជាមួយនឹង Browser URL តាមទម្រង់ Declarative។
- ចាប់ពី Version 6 ឡើងទៅ React Router គាំទ្រ Nested Routes, Data Loaders, Action Handlers និង Layout Outlets យ៉ាងរលូន។
- ប្រើ package `react-router-dom` សម្រាប់ web applications។

### Code Example: React Router Setup with RouterProvider

```jsx
// 1. Install react-router-dom:
// npm install react-router-dom

import React from 'react';
import { createBrowserRouter, RouterProvider, Outlet, Link } from 'react-router-dom';

function RootLayout() {
  return (
    <div className="min-h-screen bg-slate-950 text-white">
      <header className="p-4 border-b border-slate-800 flex gap-4">
        <Link to="/">Overview</Link>
        <Link to="/courses">Courses</Link>
      </header>
      <main className="p-6">
        <Outlet /> {/* Renders child route component */}
      </main>
    </div>
  );
}

const router = createBrowserRouter([
  {
    path: '/',
    element: <RootLayout />,
    children: [
      { index: true, element: <h2>Home Dashboard</h2> },
      { path: 'courses', element: <h2>All Available Courses</h2> },
    ],
  },
]);

export function App() {
  return <RouterProvider router={router} />;
}
```
