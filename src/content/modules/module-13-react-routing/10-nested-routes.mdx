---
id: "m13-10"
number: "10"
title: "Nested Routes"
moduleId: "module-13"
moduleNumber: "13"
category: "Routing & Network"
summary: "ការរៀបចំ sub-routes ដែលចែករំលែក layout រួមគ្នាដោយប្រើប្រាស់ <Outlet />។"
codeTitle: "Nested Routes and Outlet Layout"
---

# Nested Routes

Nested Routes គឺជាបច្ចេកទេសរៀបចំ Route ជាថ្នាក់កូនចៅ ដែលអនុញ្ញាតឱ្យ Parent Route អាចចែករំលែក Layout រួម (ដូចជា Header, Sidebar, ឬ Footer) ទៅឱ្យ Child Routes ទាំងអស់។ Component `<Outlet />` ដើរតួជាកន្លែងទំនេរ (placeholder) សម្រាប់ render កូនចៅដែលត្រូវគ្នានឹង URL។

### Key Concepts & Rules:

- ជួយជៀសវាងការសរសេរ Layout components ស្ទួនៗគ្នានៅគ្រប់ទំព័រ។
- នៅពេលដែល user ផ្លាស់ប្តូរ sub-routes មានតែផ្នែកខាងក្នុង `<Outlet />` ប៉ុណ្ណោះដែល re-render ចំណែក Header និង Sidebar នៅរក្សាទុកដដែល (គ្មាន flickering)។
- ប្រើ `index` attribute សម្រាប់កំណត់ default view នៅពេលដែល path ត្រូវគ្នានឹង parent ផ្ទាល់។

### Code Example: Nested Routes and Outlet Layout

```jsx
// Route configuration:
<Route path="/dashboard" element={<DashboardLayout />}>
  <Route index element={<DashboardOverview />} />
  <Route path="analytics" element={<AnalyticsView />} />
  <Route path="settings" element={<SettingsView />} />
</Route>

// Inside DashboardLayout.jsx:
export function DashboardLayout() {
  return (
    <div className="flex">
      <Sidebar />
      <main className="flex-1 p-6">
        <Outlet /> {/* Child route renders here! */}
      </main>
    </div>
  );
}
```
